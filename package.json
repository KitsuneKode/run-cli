{
  "name": "run-cli",
  "version": "0.1.0",
  "description": "A lightweight Bun-native CLI for running projects through local profiles and smart detection.",
  "type": "module",
  "private": true,
  "module": "src/cli.ts",
  "bin": {
    "run": "bin/run.ts",
    "runx": "bin/run.ts"
  },
  "files": ["bin", "src", "docs", "README.md"],
  "engines": {
    "bun": ">=1.3.9"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "mkdir -p dist && bun build src/cli.ts --outfile dist/cli.js --target bun",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "biome check . && markdownlint-cli2 README.md docs/**/*.md",
    "format": "biome format --write . && markdownlint-cli2 --fix README.md docs/**/*.md",
    "check": "bun run lint && bun test",
    "link:global": "bun link",
    "relink:global": "bun run scripts/relink-global.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@types/bun": "latest",
    "@biomejs/biome": "^1.9.4",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "markdownlint-cli2": "^0.18.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
